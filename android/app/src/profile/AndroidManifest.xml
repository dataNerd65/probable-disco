<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yourapp">

    <!-- Add this meta-data inside the <application> tag -->
    <application
        ...
        >

        <!-- Add the flutter_config meta-data here -->
        <meta-data
            android:name="flutter_config"
            android:resource="@xml/flutter_config" />

        <!-- Ensure you have INTERNET permission for Flutter -->
        <uses-permission android:name="android.permission.INTERNET"/>
        
        <!-- Other configurations for your application -->

    </application>
</manifest>
